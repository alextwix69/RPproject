[project]
name = "rpproject"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.0.1",
    "python-telegram-bot>=21.0",
    "SQLAlchemy>=2.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0",
]

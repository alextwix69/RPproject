"""
Файл делает директорию src Python-пакетом.

Это нужно, чтобы Python мог корректно импортировать модули из src.

Например, благодаря этому можно писать импорты вроде:

from src.core.config import settings

или:

from src.handlers.start import router

Обычно внутри init.py может вообще не быть кода.

Даже пустой файл всё равно выполняет архитектурную роль: он показывает Python, что эта директория является частью приложения.

Для Telegram-бота src — это основная директория с кодом, поэтому init.py помогает нормально организовать импорты между handlers, services, models, repositories и другими частями системы.
"""
"""
Файл делает директорию src Python-пакетом.

Это нужно, чтобы модули приложения можно было импортировать через src:
handlers, services, models, repositories, core, keyboards и другие части
Telegram-бота.
"""
